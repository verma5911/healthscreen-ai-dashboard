# HealthScreen AI Dashboard

Standalone React/Vite dashboard for the Adaptive Multi-Sensor Health Screening Framework.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The dashboard currently runs in demo mode with mock sensor data. Raspberry Pi 3B+ / ESP32-CAM integration can be added later through an API layer.
